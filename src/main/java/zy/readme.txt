项目:
  1. 文件搜索
重点:   递归搜索, 多线程的使用
技能:    每个目录及子目录 的递归 放到一个线程中.
            Queue   -> ConcurrentLinkedQueue.poll()     同步队列.
============================================================
  2. 矩阵乘法
重点：　线程划分的粒度
结论:  线程并不是越多越好, 要注意线程切换的时间和空间的开销，所以线程划分的粒度很重要.
============================================================
   3. 聊天机器人客服
重点:    Socket与ServerSocket的使用, 线程的划分.
            Socket与HttpURLConnection的对比.    ->  扩展：    如代理 , 过滤   HttpClient    ->   框架
============================================================
   4. ATM机模拟
重点:
        客户端+服务端的开发,架构思想.
        自定义协议.      Socket与ServerSocket的使用, 线程的划分.
         jdbc的使用.
============================================================
   3. 迅雷下载
下载步骤 ：
1. 先只取文件大小，创建本地空文件.
2. 再按线程分段下载.

对于步骤一:   Http协议
请求头->method  ->  GET/POST/   HEAD   /DELETE/PUT/TRACE/OPTION
  采用 HEAD    -> 它告诉服务器，只取文件 的Content-Length, 不下载内容

创建本地空文件. RandomAccessFile: 随机访问文件. (可访问文件的任意位置 ) | 顺序访问
        raf. seek(   字节数  )      setLength( 大小 )

计算每段大小.   下载的位置如何通知服务器
    Range: bytes=start-end

如何在主线程中获知每个子线程的下载量，并累加.   -> 回调机制+synchronized/AtomicLong
===========================================================
   6. tomcat开发:  服务器
